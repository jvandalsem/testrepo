import requests
import json
import urllib

# GroupMe (wrapper), Facebook, Instagram, Spotify, Github
# Grab day of week for each interaction and time of day
# Cache ---> must work with cache (turn internet off), databases (look at datapoints)
# Just need to submit cache and databases
